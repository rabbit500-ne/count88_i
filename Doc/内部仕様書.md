オセロ棋譜数カウントシステム 内部仕様書

# システムアーキテクチャ
```
[ブラウザ（htmx UI + 計算ロジック）] ←→ [APIサーバ（UI + API統合）] ←→ [Valkey] ←→ [タスク生成サーバ]
                                    ↓
                                  [DB]
```

URL空間の分離:
- `/ui/*`: htmx用のHTMLフラグメント専用エンドポイント
- `/api/*`: JSON API専用エンドポイント（将来のReact移行を見据える）

# プロジェクト構成
## ディレクトリ構造
```
count88_i/
│
├── .github/                        # GitHub設定
│   ├── workflows/                  # GitHub Actions
│   │   ├── ci-api.yml
│   │   ├── ci-task.yml
│   │   └── deploy.yml
│   ├── PULL_REQUEST_TEMPLATE.md
│   └── ISSUE_TEMPLATE/
│
├── .gitignore
├── README.md
├── LICENSE
├── docker-compose.yml              # ローカル開発環境
├── docker-compose.prod.yml         # 本番相当環境
│
├── api-server/                     # APIサーバ（UI + API統合）
│   ├── .gitignore
│   ├── pyproject.toml             # uv設定
│   ├── README.md
│   ├── Dockerfile
│   │
│   ├── src/
│   │   ├── __init__.py
│   │   ├── main.py                # FastAPIアプリ
│   │   ├── config.py              # 設定管理
│   │   │
│   │   ├── ui/                    # UI層（htmx + Jinja2）
│   │   │   ├── __init__.py
│   │   │   ├── router.py          # UIルーター
│   │   │   ├── csrf.py            # CSRF保護
│   │   │   └── views/             # Jinja2テンプレート
│   │   │       ├── base.html      # ベーステンプレート
│   │   │       ├── index.html     # トップページ
│   │   │       ├── _task_row.html # タスク行フラグメント
│   │   │       ├── _progress.html # 進捗フラグメント
│   │   │       └── _statistics.html # 統計フラグメント
│   │   │
│   │   ├── api/                   # API層（JSON API）
│   │   │   ├── __init__.py
│   │   │   ├── router.py          # APIルーター
│   │   │   ├── schemas.py         # Pydanticスキーマ
│   │   │   └── v1/
│   │   │       ├── __init__.py
│   │   │       ├── task.py        # タスクAPI
│   │   │       ├── result.py      # 結果API
│   │   │       └── progress.py    # 進捗API
│   │   │
│   │   ├── usecase/               # ユースケース層（ビジネスロジック）
│   │   │   ├── __init__.py
│   │   │   ├── task_usecase.py    # タスク管理ユースケース
│   │   │   ├── result_usecase.py  # 結果処理ユースケース
│   │   │   └── progress_usecase.py # 進捗管理ユースケース
│   │   │
│   │   ├── domain/                # ドメイン層（ビジネスルール）
│   │   │   ├── __init__.py
│   │   │   ├── task.py            # タスクドメインモデル
│   │   │   ├── result.py          # 結果ドメインモデル
│   │   │   └── position.py        # 局面ドメインモデル
│   │   │
│   │   ├── infra/                 # インフラ層（実装詳細）
│   │   │   ├── __init__.py
│   │   │   ├── repositories/      # リポジトリ実装
│   │   │   │   ├── __init__.py
│   │   │   │   ├── task_repository.py
│   │   │   │   └── result_repository.py
│   │   │   ├── models/            # SQLAlchemyモデル
│   │   │   │   ├── __init__.py
│   │   │   │   ├── task.py
│   │   │   │   ├── result.py
│   │   │   │   └── progress.py
│   │   │   ├── queue/             # Valkeyクライアント
│   │   │   │   ├── __init__.py
│   │   │   │   └── client.py
│   │   │   └── database/          # DB設定
│   │   │       ├── __init__.py
│   │   │       ├── session.py     # セッション管理
│   │   │       └── migrations/    # Alembic
│   │   │
│   │   ├── static/                # 静的ファイル（計算ロジック）
│   │   │   ├── js/                # JavaScript
│   │   │   │   ├── workers/       # Web Workers
│   │   │   │   │   ├── bfs.worker.js
│   │   │   │   │   ├── dfs.worker.js
│   │   │   │   │   └── shared.js
│   │   │   │   ├── wasm/          # WebAssembly
│   │   │   │   │   └── othello.wasm
│   │   │   │   ├── bitboard.js    # ビットボード操作
│   │   │   │   └── calculator.js  # 計算エンジン
│   │   │   ├── css/               # スタイルシート
│   │   │   └── favicon.ico
│   │   │
│   │   └── utils/                 # ユーティリティ
│   │       ├── __init__.py
│   │       ├── logger.py
│   │       └── validator.py
│   │
│   └── tests/                     # テスト
│       ├── __init__.py
│       ├── conftest.py
│       ├── unit/
│       ├── integration/
│       └── ui/                     # UI層のテスト
│           └── test_views.py
│
├── task-generator/                 # タスク生成サーバ
│   ├── .gitignore
│   ├── pyproject.toml
│   ├── README.md
│   ├── Dockerfile
│   │
│   ├── src/
│   │   ├── __init__.py
│   │   ├── main.py                # サーバ起動
│   │   ├── config.py
│   │   │
│   │   ├── generator/             # タスク生成ロジック
│   │   │   ├── __init__.py
│   │   │   ├── initial.py         # 初期タスク生成
│   │   │   ├── intermediate.py    # 中間タスク生成
│   │   │   └── strategy.py        # 分割戦略
│   │   │
│   │   ├── algorithms/            # アルゴリズム
│   │   │   ├── __init__.py
│   │   │   ├── bfs.py             # BFS実装
│   │   │   ├── dfs.py             # DFS実装（検証用）
│   │   │   └── bitboard.py        # ビットボード操作
│   │   │
│   │   ├── valkey/                # Valkeyクライアント
│   │   │   ├── __init__.py
│   │   │   └── client.py
│   │   │
│   │   ├── database/              # DB接続
│   │   │   ├── __init__.py
│   │   │   └── client.py
│   │   │
│   │   └── utils/
│   │       ├── __init__.py
│   │       ├── zobrist.py         # Zobrist Hashing
│   │       └── transposition.py   # 転置表
│   │
│   └── tests/
│       ├── __init__.py
│       ├── test_bfs.py
│       └── test_bitboard.py
│
├── shared/                         # 共通コード
│   ├── python/                    # Python共通ライブラリ
│   │   ├── pyproject.toml
│   │   └── othello_core/
│   │       ├── __init__.py
│   │       ├── bitboard.py        # ビットボード基本操作
│   │       ├── position.py        # 局面表現
│   │       └── constants.py       # 定数定義
│   │
│   └── typescript/                # TypeScript共通ライブラリ
│       ├── package.json
│       └── src/
│           ├── types.ts           # 型定義
│           ├── constants.ts
│           └── utils.ts
│
├── scripts/                        # 開発用スクリプト
│   ├── setup.sh                   # 初期セットアップ
│   ├── dev-start.sh               # 開発環境起動
│   ├── test-all.sh                # 全テスト実行
│   ├── db-migrate.sh              # DBマイグレーション
│   └── benchmark.sh               # ベンチマーク実行
│
└── infra/                          # インフラ設定
    ├── docker/                    # Docker設定
    ├── postgresql/                # PostgreSQL設定
    ├── valkey/                    # Valkey設定
    ├── nginx/                     # リバースプロキシ
    └── k8s/                       # Kubernetes（本番用）
```

## コンポーネント間の依存関係
```
┌─────────────────────┐
│ Browser (htmx)      │
│  + 計算ロジック(JS/WASM) │
└──────────┬──────────┘
           │ HTTP
           │ /ui/* (HTML) /api/* (JSON)
           ↓
┌─────────────────────┐      ┌──────────────┐
│   API Server        │ ←──→ │   Valkey     │
│  (UI + API統合)      │      └──────┬───────┘
└──────────┬──────────┘              │
           │                         │
           ↓                         ↓
┌─────────────────────┐      ┌──────────────┐
│    PostgreSQL       │      │Task Generator│
└─────────────────────┘      └──────────────┘
```

設計原則:
- Usecase層が唯一の実行主体（UI/API共通）
- Domain/Repository層は完全共有

# 技術スタック
各モジュールで使用する主要技術スタックの概要（モジュール間の連携に必要な情報）:
- api-server: Python 3.12+, FastAPI, SQLAlchemy 2.0, redis-py
- task-generator: Python 3.12+, NumPy, redis-py
- shared: Python共通ライブラリ、TypeScript共通ライブラリ
- infra: PostgreSQL 16+, Valkey 7.2+, Docker, Kubernetes

詳細は各モジュール設計書を参照。

# データフロー設計
## タスクライフサイクル
```
[タスク生成サーバ]
   │
   │ 1. タスク生成
   ↓
[Valkey Queue]
   │
   │ 2. タスク取得 (GET /api/v1/task)
   ↓
[ブラウザ（htmx UI + 計算ロジック）]
   │
   │ 3. 計算実行 (BFS/DFS) - JavaScript/WebAssembly
   ↓
[APIサーバ]
   │
   │ 4. 結果保存 (POST /api/v1/result) - JSON API
   │ 5. UI更新 (GET /ui/progress) - HTMLフラグメント
   ↓
[PostgreSQL]
   │
   │ 6. 結果集約
   ↓
[タスク生成サーバ]
   │
   │ 7. 次フェーズのタスク生成
   ↓
...
```

通信の分離:
- 計算結果の送信: `/api/v1/result` (JSON API)
- UI表示の更新: `/ui/progress` (HTMLフラグメント via htmx)

## シーケンス図：タスク取得
```
Browser         APIServer       Valkey          Database
  │                │              │                │
  │──GET /api/task→│              │                │
  │                │──RPOP────────→│                │
  │                │←─task_data───┘                │
  │                │──SELECT task─────────────────→│
  │                │←─task_details────────────────┘│
  │                │──SETEX lock──→│                │
  │←─200 OK───────┤                │                │
  │   {task: JSON} │                │                │
```

UI表示の更新:
```
Browser         APIServer       Database
  │                │                │
  │──GET /ui/tasks→│                │
  │                │──SELECT tasks──→│
  │                │←─task_list─────┘
  │←─200 OK───────┤                │
  │   <HTML>       │                │
```

## シーケンス図：結果送信
```
Browser         APIServer       Valkey          Database
  │                │              │                │
  │──POST /api/result→│              │                │
  │   {result: JSON} │              │                │
  │                │──DEL lock────→│                │
  │                │──INSERT result──────────────→│
  │                │──UPDATE task─────────────────→│
  │                │←─success──────────────────────┘│
  │                │──LPUSH next_task──→│           │
  │←─200 OK───────┤              │                │
  │   {status: JSON}│              │                │
```

UI更新（進捗表示）:
```
Browser         APIServer       Database
  │                │                │
  │──GET /ui/progress→│                │
  │   (htmx)        │                │
  │                │──SELECT stats──→│
  │                │←─progress_data─┘
  │←─200 OK───────┤                │
  │   <div>...</div>│                │
  │   (HTML fragment)│                │
```

# データベース設計
## スキーマ定義
### tasksテーブル
```sql
CREATE TABLE tasks (
    task_id BIGSERIAL PRIMARY KEY,
    task_type VARCHAR(10) NOT NULL CHECK (task_type IN ('BFS', 'DFS')),
    phase VARCHAR(50) NOT NULL, -- 'phase1_initial', 'phase2_intermediate', 'phase3_final'
    parent_task_id BIGINT REFERENCES tasks(task_id),
    
    -- 局面データ
    position_black BYTEA NOT NULL, -- 黒石ビットボード (8 bytes)
    position_white BYTEA NOT NULL, -- 白石ビットボード (8 bytes)
    turn CHAR(1) NOT NULL CHECK (turn IN ('B', 'W')),
    depth INT NOT NULL,
    
    -- ステータス管理
    status VARCHAR(20) NOT NULL DEFAULT 'pending' 
        CHECK (status IN ('pending', 'processing', 'completed', 'failed', 'timeout')),
    priority INT DEFAULT 100, -- 優先度（高いほど優先）
    retry_count INT DEFAULT 0,
    
    -- クライアント情報
    assigned_to VARCHAR(100),
    assigned_at TIMESTAMP,
    
    -- タイムスタンプ
    created_at TIMESTAMP DEFAULT NOW(),
    started_at TIMESTAMP,
    completed_at TIMESTAMP,
    
    -- インデックス
    INDEX idx_status (status),
    INDEX idx_phase (phase),
    INDEX idx_assigned (assigned_to, assigned_at)
);
```

### resultsテーブル
```sql
CREATE TABLE results (
    result_id BIGSERIAL PRIMARY KEY,
    task_id BIGINT REFERENCES tasks(task_id) ON DELETE CASCADE,
    result_type VARCHAR(10) NOT NULL CHECK (result_type IN ('BFS', 'DFS')),
    
    -- タスク結果（汎用形式、JSONB形式）
    task_answer JSONB NOT NULL,
    -- 例（DFS）: [{"label": "game_count", "value": "123456789012345678901234567890"}]
    -- 例（BFS）: [{"label": "child_positions", "value": [{"position": {...}, "path_count": 1}, ...]}]
    
    -- 後方互換性のための個別カラム（非推奨、将来削除予定）
    game_count NUMERIC(80, 0), -- 最大80桁の整数（task_answerから抽出可能）
    child_positions JSONB, -- BFS結果の子局面リスト（task_answerから抽出可能）
    
    -- メタデータ
    computation_time FLOAT, -- 秒
    client_id VARCHAR(100),
    client_info JSONB,
    
    -- 検証
    verified BOOLEAN DEFAULT FALSE,
    verification_count INT DEFAULT 0,
    
    created_at TIMESTAMP DEFAULT NOW(),
    
    INDEX idx_task_id (task_id),
    INDEX idx_verified (verified)
);
```

### progressテーブル
```sql
CREATE TABLE progress (
    id SERIAL PRIMARY KEY,
    phase VARCHAR(50) NOT NULL,
    
    -- タスク統計
    total_tasks BIGINT NOT NULL DEFAULT 0,
    completed_tasks BIGINT NOT NULL DEFAULT 0,
    failed_tasks BIGINT NOT NULL DEFAULT 0,
    
    -- 棋譜数集計
    total_games NUMERIC(80, 0) DEFAULT 0,
    
    -- パフォーマンス指標
    tasks_per_second FLOAT,
    avg_computation_time FLOAT,
    
    -- クライアント情報
    active_clients INT DEFAULT 0,
    
    -- 推定
    estimated_completion TIMESTAMP,
    
    updated_at TIMESTAMP DEFAULT NOW()
);
```

### client_sessionsテーブル
```sql
CREATE TABLE client_sessions (
    session_id VARCHAR(100) PRIMARY KEY,
    
    -- クライアント情報
    user_agent TEXT,
    ip_address INET,
    
    -- パフォーマンス
    completed_tasks INT DEFAULT 0,
    total_computation_time FLOAT DEFAULT 0,
    avg_task_time FLOAT,
    
    -- ステータス
    last_active TIMESTAMP DEFAULT NOW(),
    created_at TIMESTAMP DEFAULT NOW(),
    
    INDEX idx_last_active (last_active)
);
```

## Valkeyデータ構造
### キュー構造
```
# BFSタスクキュー（List）
task_queue:bfs:phase1 = [task_id_1, task_id_2, ...]
task_queue:bfs:phase2 = [...]

# DFSタスクキュー（List）
task_queue:dfs:phase3 = [task_id_100, task_id_101, ...]

# 処理中タスクロック（Hash, TTL: 60秒）
task_lock:{task_id} = {
    "client_id": "browser-abc123",
    "started_at": "2025-11-18T10:00:00Z"
}

# クライアントセッション（Hash, TTL: 300秒）
client:{client_id} = {
    "current_task": "1234567890",
    "last_heartbeat": "2025-11-18T10:00:00Z"
}

# 進捗カウンター（String）
progress:completed = "1234567"
progress:total = "10000000"

# 統計（Sorted Set）
stats:computation_time = {
    "task_id_1": 5.234,
    "task_id_2": 3.456,
    ...
}
```

# カウントアルゴリズム
## 基本戦略
探索木を階層的に分割し、幅優先探索（BFS）で並列化ポイントを作り、深さ優先探索（DFS）で終局までカウントする。

## 幅優先探索（BFS）
目的: 局面ツリーを特定深さまで展開し、並列計算可能なタスクに分割

アルゴリズム:
```
function BFS(start_position, target_depth):
    queue = [start_position]
    result_positions = {}
    
    for depth in 0 to target_depth:
        next_queue = []
        for position in queue:
            for move in legal_moves(position):
                next_pos = apply_move(position, move)
                if next_pos not in result_positions:
                    result_positions[next_pos] = 0
                result_positions[next_pos] += 1
                next_queue.append(next_pos)
        queue = next_queue
    
    return result_positions  # {局面: 到達パス数}
```

パラメータ:
- `start_position`: 開始局面（64bit ビットボード × 2）
- `target_depth`: 展開する深さ（推奨: 6-8手）

出力:
- 到達可能局面のリスト
- 各局面への到達パス数（重複カウント用）

計算量: O(b^d)
- b: 平均分岐数（約10）
- d: 探索深さ

## 深さ優先探索（DFS）
目的: 指定局面から終局までの棋譜総数をカウント

アルゴリズム:
```
function DFS(position, memo={}):
    if position in memo:
        return memo[position]
    
    if is_game_over(position):
        return 1
    
    legal = legal_moves(position)
    if len(legal) == 0:  # パス
        return DFS(pass_turn(position), memo)
    
    total = 0
    for move in legal:
        next_pos = apply_move(position, move)
        total += DFS(next_pos, memo)
    
    memo[position] = total
    return total
```

パラメータ:
- `position`: 開始局面

出力:
- 終局までの棋譜総数（BigInt / 多倍長整数）

最適化:
- メモ化（転置表）
- 枝刈り（明らかに無効な手を排除）

## 分散計算フロー
目的：深いセグメントを常時保持せず、**D48-54 を作ってDFSし、親（D42-48）へ「棋譜数（スカラー）」だけを残して破棄**する、という流れを上位セグメントまで繰り返して総数を得る。

表記：
- 深さは手番数（「着手」または「パス(見送り)」）で数え、スタート地点をD0とする。
- `D00-06` は D0 から D6 までの深さ帯（6手幅）を意味する（以下同様に `D06-12`, `D12-18` ...）。
- 幅探索（BFS）で作成した「深さ帯ごとの探索木データ」を探索セグメントと呼ぶ。
- セグメント幅は **6手固定** とする（妥当とする）。
- 局面は **マージ（同一局面を統合）** し、同一局面に複数経路で到達した場合は **到達パス数を加算** する。
  - 到達パス数 = 「（セグメントの起点深さ）からその局面に到達する手順数（着手＋パスを含む）」。
重要：
- パスが挟まるため、深さ（手番数）はD60を超える可能性がある。

対象セグメント（例）：
`D00-06` → `D06-12` → … → `D42-48` → `D48-54`

### フェーズ1: `D00-06` セグメント作成（起点の用意）
1. ルート局面（初期配置）から **`D00-06`** をBFSで作成する。
2. 深さD6（`D00-06` の終端）にある各ノード（局面）について、以降の計算のために以下を保持する。
   - 局面（ビットボード等）
   - ルートからの到達パス数（マージ済み）

### フェーズ2: 1ノードずつ深掘りして `D48-54` をDFSで解く（葉セグメントは破棄）
1. まず対象ノードを1つ選び、`D06-12` セグメントをBFSで作成する。
2. さらにその中の対象ノードを1つ選び、同様に `D12-18` … を繰り返し、最終的に **`D48-54` セグメント** を作成する。
3. `D48-54` の各ノードについて、1クライアント上でDFSを実施し「そのノードから終局までの棋譜数」を求める。

### フェーズ3: 親セグメントへ圧縮（棋譜数だけ残す）し、`D48-54` を順次作り直す
1. ある `D42-48` ノードのために作った `D48-54` が全て完了したら、`D42-48` ノードの棋譜数を以下で確定する。

```
（D42-48ノードの棋譜数）
  = Σ( D48-54各葉への到達パス数 × その葉からの棋譜数 )
```

2. 完了した `D42-48` ノードについては **棋譜数（スカラー）だけを記録** し、`D48-54` セグメントはメモリ削減のため（いずれ）破棄する。
3. 次の `D42-48` ノードへ進み、再びそのノードから `D48-54` セグメントを作成→DFS→親へ圧縮、を繰り返す。

### フェーズ4: 上位セグメントへ繰り返し適用し、`D00-06` を埋めて総数を得る
1. `D42-48` の全ノードが棋譜数確定したら、同様の要領で `D36-42`（→`D42-48`）を処理する。
   - このとき `D42-48` 側は「棋譜数（スカラー）」が得られているため、下位（`D48-54`）を再生成せずに集約できる。
2. これを上へ繰り返し、最終的に `D00-06`（深さD6フロンティア）の全ノードに棋譜数を埋める。
3. ルートからの到達パス数で重み付けして合計し、総棋譜数（答え）とする。

```
総棋譜数 = Σ( D6各ノードの到達パス数 × そのノードの棋譜数 )
```

## task-generatorのタスク生成方針
### (1) child生成のコントロール
  - BFSを実行するセグメントは、実行しているセグメント（current）と、次のDFSを実行するセグメント（next）の2つを生成するようにする。
  - ただし、DFSを実行するセグメント(D48-54)は全件タスク化する

### (2) current/next の選択ルール（暫定）
  - D42-48層の候補から **`task_id` 昇順**で選ぶ。
    - current: 最小`task_id`
    - next: 次点の`task_id`

### (3) 決定論性を担保するための運用前提（重要）
  - `task_id` はDBの採番（INSERT順）に依存するため、以下を前提とする。
  - task-generatorは **単一プロセス（単一インスタンス）で稼働**させる（多重起動しない）
  - 並列INSERTを発生させない（複数ワーカー/並列バッチで同一テーブルへ同時投入しない）
  - 候補のINSERT順が毎回同一になること（例: `child_positions` の走査順が固定）
  - 再実行/リトライ時に「新規INSERTでtask_idを増やす」挙動を避け、冪等に再開できる運用/実装とする

## パラメータ調整指針
| パラメータ | 推奨値 | 考慮点 |
|----------|--------|--------|
| セグメント幅 | 6手 | メモリ（例: 10MB以下）に収まるように調整 |
| DFS開始セグメント | D48-54 | 1ノード（局面）あたりの計算時間（目安: 1-10秒） |
| タスクタイムアウト | 60秒 | ネットワーク遅延を考慮 |

ベンチマーク必須: 実環境で最適値を測定

# データ構造の内部実装
## ビットボード実装
局面は64bitビットボード×2で表現します。黒石と白石それぞれを64bit整数で表現し、1が石がある位置、0が空き位置を表します。

ビットボード操作:
- 合法手の生成: ビット演算で高速に実現
- 着手処理: XOR演算で石の反転を実現
- 局面比較: ハッシュ値で高速比較

## 多倍長整数の扱い
JavaScript: `BigInt`型を使用
Python: 組み込み`int`型（自動的に多倍長整数として扱われる）
データベース: `NUMERIC(80, 0)`型（最大80桁の整数）

API通信時は文字列形式で送受信し、JSON互換性を確保します。

# 実装上の課題と対策
## メモリ使用量の制御
課題: BFS展開時にメモリ不足が発生する（特にフェーズ2の中間分割）

対策:
- ストリーミング処理の採用
  - 全局面を一度にメモリ展開しない
  - `current`（処理中）と`next`（次のタスク）のみを保持
  - 処理済みタスクは即座にキューへ投入し解放
- 段階的処理
  - バッチサイズを制限（例: 1000タスク/バッチ）
  - メモリ使用量の監視とダイナミック調整
- ディスク活用
  - 大量の中間結果は一時的にDBまたはファイルに保存
  - 必要に応じてページング処理

## 重複局面の排除
課題: 異なる手順で同一局面に到達

対策:
- Zobrist Hashing による高速比較
- 転置表（Transposition Table）の活用

## 計算結果の検証
課題: クライアント側の計算ミスや不正

対策:
- ランダムに5-10%のタスクを複数クライアントに重複配布
- 結果が一致しない場合は再計算

## タスク失敗時のリトライ
課題: ネットワーク断やブラウザクローズ

対策:
- タスクタイムアウト（60秒）後に自動再配布
- 3回失敗したタスクは優先度下げ

## 数値オーバーフロー
課題: 10^58 は64bit整数を超える

対策:
- BigInt / 多倍長整数の使用
- 文字列での転送（JSON互換性）

# セキュリティ設計
## 認証・認可
### Phase 1（プロトタイプ）
- 認証なし（オープン参加）
- Client ID による簡易識別

### Phase 2（将来）
- APIキー認証: クライアントごとにAPIキー発行
- レート制限: IP/クライアントIDごとに制限
- 不正検知: 異常な計算結果を自動検出

## データ検証
### 結果の検証戦略
1. ランダム検証: 5-10%のタスクを複数クライアントに重複配布
2. 統計的検証: 計算時間が異常に短い結果を再検証
3. ブラックリスト: 複数回不正結果を送信したクライアントをブロック

詳細は各モジュール設計書を参照。

# 監視・ログ設計
## ログレベル
| レベル | 用途 |
|--------|------|
| DEBUG | 開発時の詳細情報 |
| INFO | 正常な動作記録 |
| WARNING | 注意が必要な状況 |
| ERROR | エラー発生 |
| CRITICAL | システム停止レベル |

## ログフォーマット（JSON）
```json
{
  "timestamp": "2025-11-18T10:00:00.123Z",
  "level": "INFO",
  "service": "api-server",
  "message": "タスクを取得しました",
  "context": {
    "task_id": "1234567890",
    "client_id": "browser-abc123",
    "task_type": "BFS"
  },
  "trace_id": "abc123-def456"
}
```

## メトリクス（Prometheus形式）
```
# タスク処理速度
count88_tasks_processed_total{service="api", type="BFS"} 12345

# 平均処理時間
count88_task_duration_seconds{service="api", type="DFS"} 5.234

# アクティブクライアント数
count88_active_clients 42

# キュー長
count88_queue_length{queue="bfs"} 1000000
```

詳細は各モジュール設計書を参照。

# 開発ルール
## コードスタイル
### Python
- フォーマッター: `black`
- リンター: `ruff`
- 型チェック: `mypy`
- ドキュメント: Google Style Docstrings

### TypeScript
- フォーマッター: `prettier`
- リンター: `eslint`
- スタイルガイド: Airbnb

## テスト戦略
| 種別 | カバレッジ目標 | 実行タイミング |
|------|---------------|---------------|
| 単体テスト | 80%以上 | コミット時 |
| 統合テスト | 主要フロー | PR時 |
| E2Eテスト | クリティカルパス | デプロイ前 |

## レビュールール
- [ ] コードが要件を満たしている
- [ ] テストが追加されている
- [ ] ドキュメントが更新されている
- [ ] lintエラーがない
- [ ] 型エラーがない
- [ ] セキュリティ上の問題がない

